# Context

- **Platform**: WebApp (Astro + React + Supabase)
- **Environment**: Cloudflare Workers
- **Goal**: Implement member portal featuring OTP login, dynamic downloadable ID card, Razorpay SDK integration (test mode), and referral tracking.
- **Dependencies to install**: `react-qr-code`, `html2canvas`, and `razorpay` (or frontend SDK/types as needed).
- **Integrity Mode**: `demo` (which permits simulated operations but requires complete codebase integrity).
